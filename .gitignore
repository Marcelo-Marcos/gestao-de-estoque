node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
*.log
.vite
*.tsbuildinfo

# Quadro de design: versionamos as fontes (.dc.html e canvas.json), não a
# saída do gerador — ela embute o editor inteiro e passa de 2 MB.
.design/*.html
!.design/*.dc.html
